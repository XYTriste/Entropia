# 多周日期支持功能测试计划

> 本文件面向手动测试人员。请按照以下顺序逐项验证。

---

## 前置准备

1. 确保后端服务已启动（`localhost:8000`）
2. 确保前端已编译/热更新（`localhost:3000`）
3. 建议先执行 `make init-db` 或确认数据库中已有模板时段数据（20 条 `exam_date` 为空的记录）

---

## 一、基础数据 → 时段管理

### 1.1 时段列表初始状态
- [ ] 打开「基础数据」→「时段管理」
- [ ] 确认列表中每条时段都显示 `exam_date`（考试日期）列
- [ ] **首次进入时**：应看到 20 条模板记录（`exam_date` 列为空或 `-`）

### 1.2 生成多周时段（核心功能）
- [ ] 选择起始日期 **必须是周一**，例如 `2026-06-02`
- [ ] 选择周数 `1`，点击「生成考试时段」
  - [ ] 列表中应新增 20 条记录，共 40 条（含模板）
  - [ ] 新增记录的 `exam_date` 为 06-02 ~ 06-06（周一至周五）
- [ ] 再次选择周数 `2`，点击「生成考试时段」
  - [ ] 列表中应新增 40 条记录，共 60 条（含模板）
  - [ ] 第一周：06-02 ~ 06-06；第二周：06-09 ~ 06-13
- [ ] 再次选择周数 `4`，点击「生成考试时段」
  - [ ] 列表中应新增 80 条记录，共 100 条（含模板）
  - [ ] 4 周的日期依次正确排列
- [ ] **验证模板记录始终保留**：前 20 条 `exam_date` 仍为空的记录不应被删除

### 1.3 重新生成覆盖
- [ ] 换一个起始日期（如 `2026-06-09`），点击「生成考试时段」
- [ ] 旧生成记录应被删除，新记录正确生成
- [ ] 模板记录（前 20 条）仍然保留

---

## 二、自动排考 → SchedulerView

### 2.1 配置面板新增字段
- [ ] 打开「自动排考」页面
- [ ] 配置面板中应出现两个新字段：
  - 「考试起始日期」日期选择器
  - 「考试周数」下拉框（选项为 1、2、3、4）
- [ ] 修改这两个值，点击「保存配置」
- [ ] 刷新页面，确认配置值能正确恢复

### 2.2 未生成时段时的排考拦截
- [ ] （如可能）先删除/重置时段，确保无 `exam_date IS NOT NULL` 的记录
- [ ] 选择课程后点击「开始排考」
- [ ] **验证**：后端应返回错误提示，如"无可用考试时段"或"请先生成时段"

### 2.3 正常排考流程
- [ ] 确保已生成多周时段（建议 2 周，40 条生成记录）
- [ ] 选择若干课程，点击「开始排考」
- [ ] 排考应正常完成，不报错
- [ ] 排考结果弹窗中，每场考试的时段应显示为 `"06-02 周一 T1"` 格式（含日期标签）

### 2.4 公共课映射（如有公共课数据）
- [ ] 确保某些课程的 `dept_assigned_time_slot_id` 指向模板记录
- [ ] 排考完成后，检查公共课是否被安排在**第一周**的对应时段
- [ ] 公共课不应被安排到模板记录本身（`exam_date` 为空的记录）

---

## 三、排考结果 → ResultsView

### 3.1 总览矩阵（OverviewPanel）
- [ ] 打开「排考结果」→「总览」
- [ ] 矩阵列数应等于 `生成周数 × 5`（如 2 周 → 10 列，4 周 → 20 列）
- [ ] 列标题格式应为 `"06-02 周一"`（日期 + 星期）
- [ ] 列按日期顺序从左到右正确排列
- [ ] 点击单元格展开后，考试详情中显示正确的 `exam_date`

### 3.2 各子面板日期显示一致性
逐项检查以下面板，确认所有考试卡片/表格行都显示日期标签：

- [ ] **教师** → 甘特图中每场考试显示 `"06-02 周一 T1"` 格式
- [ ] **教师** → 负荷统计中的日期列正确
- [ ] **教室** → 教室矩阵的列标题和单元格内日期正确
- [ ] **流动监考** → 监考矩阵的列标题和单元格内日期正确
- [ ] **班级** → 班级课表中每场考试显示完整日期标签
- [ ] **课程** → 课程分布面板中日期标签正确

---

## 四、手动微调 → AdjustmentsView

### 4.1 换教师弹窗日期显示
- [ ] 打开「手动微调」
- [ ] 点击某场考试的「换教师」按钮
- [ ] 弹窗中应显示完整的日期信息，如 `"2026-06-02 周一 T1"` 或 `"06-02 周一 T1"`
- [ ] 可用教师列表能正常加载，不报错
- [ ] 选择新教师后，替换成功，页面刷新后生效

### 4.2 多周歧义消除
- [ ] 生成 2 周以上时段并执行排考
- [ ] 找到第一周周一 T1 的考试，点击「换教师」
- [ ] 找到第二周周一 T1 的考试，点击「换教师」
- [ ] **验证**：两次查询的可用教师列表可能不同（因为是不同日期），但绝不应该是相同结果导致混淆

---

## 五、数据持久化验证（通过 API 或数据库）

### 5.1 时段表
```sql
SELECT id, day_of_week, slot_code, exam_date
FROM time_slots
ORDER BY id;
```
- [ ] 前 20 条 `exam_date` 为 NULL（模板）
- [ ] 后续记录 `exam_date` 有具体日期，且按周递增

### 5.2 排考配置表
```sql
SELECT exam_start_date, exam_weeks FROM schedule_configs LIMIT 1;
```
- [ ] `exam_start_date` 与前端选择的起始日期一致
- [ ] `exam_weeks` 与前端选择的周数一致

### 5.3 排考版本快照自包含
```sql
SELECT data_snapshot->'time_slots' FROM schedule_versions ORDER BY id DESC LIMIT 1;
```
- [ ] 快照中的每个 `time_slot` 对象包含 `exam_date`、`day_of_week`、`slot_code`
- [ ] 不依赖 `time_slots` 表即可独立渲染历史版本

---

## 六、边界与回归测试

| 场景 | 验证结果 |
|------|----------|
| 快速连续点击「生成时段」按钮 | [ ] 不报错，最终数据正确 |
| 生成时段后未排考，直接查看 ResultsView | [ ] 矩阵列正确显示，单元格为空 |
| 1 周 vs 4 周排考后导出 Excel | [ ] 导出数据包含正确的 `exam_date` |
| 刷新页面后所有日期显示 | [ ] 与刷新前一致，无乱码 |
| 调整考试时间后，audit log 中的日期字符串 | [ ] 包含具体日期，无歧义 |

---

## 问题记录区

测试过程中发现的问题请记录在此：

| 序号 | 页面/模块 | 操作步骤 | 预期结果 | 实际结果 | 严重程度 |
|------|-----------|----------|----------|----------|----------|
| 1 | | | | | |
| 2 | | | | | |

---

*测试计划版本：2026-05-21*
*对应功能：多周日期支持（阶段一 + 阶段二 + 阶段三）*
