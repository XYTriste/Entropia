# 考试排考系统 — 开发进度报告

> 生成时间：2026-04-28  
> 当前状态：**核心功能已跑通，支持班级拆分排考，10门课程0冲突成功排考**

---

## 一、项目概况

| 项目 | 说明 |
|------|------|
| 技术栈 | FastAPI + SQLAlchemy 2.0 + PostgreSQL 15 + Vanilla JS + Nginx |
| 部署方式 | Docker Compose |
| 核心引擎 | OR-Tools CP-SAT + 贪心分配算法 |
| 前端缓存 | `app.js?v=12` |

---

## 二、已修复/已完成的功能

### 2.1 排考引擎核心

| # | 功能 | 状态 | 说明 |
|---|------|------|------|
| 1 | 公共课排考 | ✅ | 按教务处指定日期+时段 deterministic 分配 |
| 2 | 专业课排考 | ✅ | 贪心+约束传播填充空闲时段 |
| 3 | AB卷自动分卷 | ✅ | 动态规划最小化两组人数差，班级不可拆分 |
| 4 | **教室分配（班级拆分）** | ✅ | 50人班可拆到两个28人教室（25+25），极端情况支持拆到3个教室 |
| 5 | 教室容量约束 HC-04 | ✅ | 每教室总人数 ≤ capacity |
| 6 | 混班约束 HC-03 | ✅ | 同一教室最多来自2个不同班级 |
| 7 | 固定监考分配 | ✅ | 每考场2名固定监考，优先专任教师 |
| 8 | 流动监考分配 | ✅ | 每时段3名流动监考 |
| 9 | 排考结果持久化 | ✅ | 应用版本时将快照写入 `exams`/`exam_classrooms`/`exam_teachers`/`patrol_teachers` |

### 2.2 前端页面与交互

| # | 页面 | 状态 | 说明 |
|---|------|------|------|
| 1 | 仪表盘 | ✅ | 统计卡片、最新版本号、操作记录 |
| 2 | 自动排考 | ✅ | 课程选择、策略选择、进度条、停止按钮、结果展示、应用按钮 |
| 3 | 排考结果 | ✅ | 总览矩阵、教师甘特图、教室矩阵、班级视图、课程视图 |
| 4 | 手动微调 | ✅ | 时段调整、教室更换、教师更换、教师对调 |
| 5 | 基础数据 | ✅ | 教师/教室/课程/班级/学生/专业/时段 — 增删改查+批量删除 |
| 6 | 导入导出 | ✅ | Excel模板下载、Excel批量导入、JSON/SQL/Excel导出 |
| 7 | 审计日志 | ✅ | 多维度过滤、分页（数据为空是因为尚未产生审计记录） |

### 2.3 关键 Bug 修复清单

#### 后端
- `CourseClass.__init__` 缺少 `class_id` 参数
- `Classroom.__init__` 缺少 `is_active` 参数
- `teacher_alloc` 模块绝对导入导致 `ModuleNotFoundError`
- `apply_schedule_version` 仅改状态未持久化到 `exams` 表
- snapshot 缺少 `class_ids` 导致 `ExamClassroomClass` 无法创建
- AB卷合并导致教室/教师/巡逻监考重复，触发唯一约束冲突

#### 前端
- `fetch` body 被 `defaultOptions` 覆盖（批量删除 422 的根源）
- `App.api.get` 返回 `{code, data}`，多处代码未 unwrap `data.data`
- 排考状态轮询未正确读取 `job_id`、`solve_time`、`version_id`
- `applyScheduleResult` URL 与后端路由不匹配（`/versions/{id}/apply` vs `/apply/{id}`）
- select-all checkbox 不同步
- edit modal 在 `hideModal` 之后才读取 DOM 导致空值

---

## 三、已知待优化项

| 优先级 | 事项 | 说明 |
|--------|------|------|
| P1 | 审计日志全面覆盖 | `adjustment_service` 和 `teacher_transfer` 已写入审计日志，但基础数据 CRUD 路由（教师/教室/课程/班级/学生）尚未写入 |
| P2 | 前端国际化/字符编码 | 部分接口返回中文出现乱码（控制台显示正常，前端偶尔乱码） |
| P3 | 排考引擎性能 | 当前同步执行，大数量级时应改为后台 Celery 任务 |
| P3 | 结果展示详情 | 总览矩阵仅显示课程名，可补充教室名、监考教师 |
| P3 | 数据校验增强 | Excel导入时的字段类型校验、教室容量冲突预警 |

## 四、AI 聊天助手功能（已实现）

### 4.1 运行模式

| 模式 | 说明 | 配置方式 |
|------|------|----------|
| 基础模式（默认） | 使用正则表达式进行意图识别，无需 API Key | `USE_DEEPSEEK=false`（默认） |
| DeepSeek 模式 | 使用 DeepSeek LLM 进行意图识别和工具调用 | `USE_DEEPSEEK=true` + 配置 `DEEPSEEK_API_KEY` |

### 4.2 已实现的工具

| 工具名称 | 功能 | 触发条件 |
|----------|------|----------|
| `query_classrooms` | 查询教室状态（空闲/已占用），含固定监考和流动监考教师信息 | 用户询问教室、空闲教室、教室状态、排考安排 |
| `query_teacher_assignments` | 查询教师监考安排（固定+流动），支持模糊匹配 | 用户询问某位老师、某姓老师、所有匹配老师的监考安排 |

### 4.3 功能特性

- ✅ SSE 流式输出（逐字显示，体验流畅）
- ✅ 支持多教师查询结果展示（返回 `teachers` 数组）
- ✅ 空结果拦截（工具返回空数据时直接返回，不经过 LLM，防止幻觉）
- ✅ 基础模式支持意图识别（星期、时段、教室参数自动提取）
- ✅ DeepSeek 模式支持自然语言对话和工具调用
- ✅ 角色约束（仅回答考试安排相关问题，闲聊简短回应）

---

## 五、AI 聊天助手 — 待开发功能清单

> 生成时间：2026-05-17  
> 状态：已规划，待开发  
> 文件路径：`AI_FEATURE_ROADMAP.md`（详细设计见该文件）

### 5.1 高优先级（P1）

1. **查询教师工作量** — 统计每位教师的监考场次、工作量分布
2. **冲突检测与建议** — 自动检测排考冲突，并给出解决建议
3. **多轮对话上下文** — 记住上一轮对话内容（如"他的监考安排"→ 指代前文的教师）
4. **模糊查询澄清** — "哪个张老师？系统中有3位张老师" → 列出选项让用户选择
5. **自然语言导出报表** — "导出下周的监考安排表" → 自动生成并下载 Excel
6. **教师监考报表** — 自动生成每位教师的监考安排报表
7. **班级考试通知** — 自动生成每个班级的考试通知（含时间、地点、监考教师）

### 5.2 中优先级（P2）

8. **查询教室利用率** — 统计每个教室的考试占用情况
9. **查询班级考试安排** — 查询某个班级的所有考试安排
10. **查询课程考试安排** — 查询某门课程的考试时间和地点
11. **自然语言排考** — "帮我把计算机科学的考试排到下周一上午" → 自动调用排考引擎
12. **导出报表（通用）** — "生成教师监考工作量报表" → 自动导出 Excel
13. **教师工作量分析** — 分析教师监考分布，建议调整方案
14. **排考质量评估** — 生成排考质量报告（公平性、合理性）
15. **自然语言触发手动微调** — "帮我把周一上午的考试调到下午" → 自动打开手动微调页面并预填参数
16. **自然语言导入数据** — "导入教师数据" → 自动下载模板并引导上传
17. **操作确认机制** — 执行敏感操作前确认（如"确定要调整张老师的监考吗？"）
18. **自然语言解释** — 不仅给出结果，还解释为什么（如"建议排到下周一，因为..."）
19. **教室使用报表** — 自动生成教室利用率报表
20. **监考签到表** — 自动生成监考教师签到表
21. **流动监考巡查表** — 自动生成流动监考教师的巡查安排表

### 5.3 低优先级（P3）

22. **手动调整考试** — "把张老师的监考从周一调到周二" → 自动执行调整
23. **分配教师** — "给5-201教室的考试分配一位专任教师" → 自动分配
24. **锁定/解锁考试** — "锁定所有公共课考试" → 批量操作
25. **智能排课建议** — 根据历史数据和约束，建议最优排考方案
26. **语音输入支持** — 支持语音转文字输入（如果前端可以实现）
27. **智能通知生成** — "通知所有教师下周的监考安排" → 生成通知文本（邮件/微信）

### 5.4 技术债务

28. **测试覆盖率提升** — 补充 AI 服务的单元测试和集成测试
29. **代码规范化** — 用 `black` + `isort` 格式化代码，用 `flake8` 或 `pylint` 检查代码质量
30. **预提交钩子** — 添加 pre-commit hook，自动检查代码规范

### 5.5 详细说明

详细的功能设计、实现方案、API 接口设计见 `AI_FEATURE_ROADMAP.md`（待创建）。

---

## 四、测试验证

### 最近一次端到端测试

```bash
POST /api/scheduler/run  {strategy: "full"}
→ 200 OK
→ status: "completed"
→ exams_scheduled: 10
→ violations: []

POST /api/scheduler/apply/9
→ 200 OK
→ exams=10, exam_classrooms=97, exam_teachers=133, patrol_teachers=40

GET /api/import-export/export/excel
→ 200 OK, content-length: 25,656 bytes
```

---

## 五、文件变更摘要

```
app/engine/classroom_alloc.py      # 完全重写，支持班级拆分
app/engine/scheduler.py             # 修复导入、snapshot补充class_ids
app/engine/models.py                # 无变更（仅确认字段定义）
app/routers/scheduler.py            # 修复CourseClass/Classroom构造、重写apply_schedule_version
app/services/export_service.py      # 无变更
app/static/js/app.js                # 大量unwrap修复、URL修复、字段路径修复
app/static/index.html               # 缓存版本 v=12
app/routers/courses.py              # 无变更
app/routers/students.py             # 无变更
```
