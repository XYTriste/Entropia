# AI 聊天助手 — 功能开发路线图

> 生成时间：2026-05-17  
> 作者：小白  
> 状态：已规划，待开发  
> 优先级说明：P1=高优先级，P2=中优先级，P3=低优先级

---

## 一、新增查询工具（扩展数据源）

### 1.1 查询教师工作量（P1）

**功能描述：**  
统计每位教师的监考场次、工作量分布，支持按日期范围、教师类型过滤。

**API 接口：**
- `GET /api/teachers/workload` — 返回所有教师的工作量统计
- `GET /api/teachers/{id}/workload` — 返回指定教师的工作量详情

**AI 工具定义：**
```python
{
    "name": "query_teacher_workload",
    "description": "查询教师监考工作量统计。当用户询问教师工作量、监考次数、谁监考最多时调用此工具。",
    "parameters": {
        "teacher_name": "可选，教师姓名（支持模糊匹配），未指定则返回所有教师",
        "date_from": "可选，起始日期（YYYY-MM-DD）",
        "date_to": "可选，结束日期（YYYY-MM-DD）"
    }
}
```

**实现文件：**
- `app/tools/teacher_tools.py` — 新增 `query_teacher_workload()` 函数
- `app/services/ai_service.py` — 在 TOOLS 列表中添加工具定义

---

### 1.2 查询考试安排（P1）

**功能描述：**  
按课程/班级/日期查询考试安排，返回考试时间、地点、监考教师。

**API 接口：**
- `GET /api/exams` — 支持按 course_id, class_id, day_of_week 过滤

**AI 工具定义：**
```python
{
    "name": "query_exam_schedule",
    "description": "查询考试安排。当用户询问某门课、某个班级或某天的考试安排时调用此工具。",
    "parameters": {
        "course_name": "可选，课程名称（支持模糊匹配）",
        "class_name": "可选，班级名称（支持模糊匹配）",
        "day_of_week": "可选，星期几（1-5）"
    }
}
```

**实现文件：**
- `app/tools/exam_tools.py` — 新增文件，实现 `query_exam_schedule()`
- `app/services/ai_service.py` — 在 TOOLS 列表中添加工具定义

---

### 1.3 查询教室利用率（P2）

**功能描述：**  
统计每个教室的考试占用情况，返回利用率百分比、空闲时段。

**API 接口：**
- `GET /api/classrooms/utilization` — 返回所有教室的利用率统计

**AI 工具定义：**
```python
{
    "name": "query_classroom_utilization",
    "description": "查询教室利用率统计。当用户询问教室利用率、哪些教室用得最多时调用此工具。",
    "parameters": {
        "classroom_name": "可选，教室名称（支持模糊匹配），未指定则返回所有教室"
    }
}
```

---

### 1.4 查询班级考试安排（P2）

**功能描述：**  
查询某个班级的所有考试安排，返回考试时间、地点、课程。

**AI 工具定义：**
```python
{
    "name": "query_class_exams",
    "description": "查询班级的考试安排。当用户询问某个班级什么时候考试时调用此工具。",
    "parameters": {
        "class_name": "班级名称（支持模糊匹配），如'计算机科学2023级'"
    }
}
```

---

### 1.5 查询课程考试安排（P2）

**功能描述：**  
查询某门课程的考试时间和地点。

**AI 工具定义：**
```python
{
    "name": "query_course_exam",
    "description": "查询课程的考试安排。当用户询问某门课什么时候考试、在哪里考时调用此工具。",
    "parameters": {
        "course_name": "课程名称（支持模糊匹配），如'数据结构'"
    }
}
```

---

## 二、新增操作工具（让 AI 真正干活）

### 2.1 自然语言排考（P1）

**功能描述：**  
用户用自然语言描述排考需求，AI 解析参数后调用排考引擎。

**示例对话：**
```
用户："帮我把计算机科学的考试排到下周一上午"
AI：调用排考引擎，参数：course_name="计算机科学", day_of_week=1, slot_code="T1,T2"
```

**实现难点：**
- 需要解析自然语言中的课程名、日期、时段
- 需要调用现有的排考引擎接口（`POST /api/scheduler/run`）
- 排考是长时间任务，需要异步处理

**实现方案：**
1. AI 解析用户意图，提取课程名、日期、时段
2. 调用 `POST /api/scheduler/run` 启动排考
3. 轮询排考状态（`GET /api/scheduler/status/{job_id}`）
4. 排考完成后，返回结果摘要

---

### 2.2 自然语言导出报表（P1）

**功能描述：**  
用户用自然语言请求导出报表，AI 自动生成并下载 Excel。

**示例对话：**
```
用户："导出下周的监考安排表"
AI：调用导出服务，生成 Excel，返回下载链接
```

**实现方案：**
1. AI 解析用户意图，确定导出类型（教师监考表/班级通知表/考场签到表）
2. 调用现有的导出接口（`GET /api/import-export/export/excel`）
3. 返回下载链接，前端自动下载

---

### 2.3 手动调整考试（P2）

**功能描述：**  
用户用自然语言描述调整需求，AI 调用手动微调接口。

**示例对话：**
```
用户："把张老师的监考从周一调到周二"
AI：调用调整接口，参数：teacher_name="张老师", from_day=1, to_day=2
```

**实现难点：**
- 需要解析自然语言中的教师名、原时间、新时间
- 需要调整引擎的支持（当前可能不支持直接调用）

---

### 2.4 分配教师（P2）

**功能描述：**  
用户请求分配监考教师，AI 自动分配。

**示例对话：**
```
用户："给5-201教室的考试分配一位专任教师"
AI：调用教师分配接口，参数：classroom="5-201", role="FIXED"
```

---

## 三、智能分析与建议（AI 的核心价值）

### 3.1 冲突检测与建议（P1）

**功能描述：**  
自动检测排考冲突，并给出解决建议。

**检测内容：**
- 教师时间冲突（同一教师同一时段监考两场）
- 教室容量冲突（考试人数 > 教室容量）
- 混班约束冲突（同一教室超过2个班级）

**建议生成：**
- 检测到冲突 → 分析冲突原因 → 给出调整建议（如"建议将XX考试调整到YY时段"）

**实现方案：**
1. 读取当前排考版本快照（`schedule_versions` 表）
2. 调用 `check_violations.py` 中的检测逻辑
3. 用 LLM 生成自然语言建议

---

### 3.2 教师工作量分析（P1）

**功能描述：**  
分析教师监考分布，识别工作量过高的教师，建议调整方案。

**分析内容：**
- 每位教师的监考场次
- 监考分布是否均匀
- 是否有教师超量（超过 `max_exams`）

**建议生成：**
- "张老师监考5场，超过最大值4场，建议将1场调整给李老师"
- "王老师和赵老师监考场次相差3场，建议平衡"

---

### 3.3 教室利用率优化（P2）

**功能描述：**  
分析教室使用效率，建议合并/拆分考场。

**分析内容：**
- 每个教室的利用率
- 是否有教室空闲过多
- 是否有教室容量浪费（如50人考场只坐20人）

**建议生成：**
- "5-201教室利用率仅30%，建议将5-202的考试合并到5-201"
- "6-301教室容量50人，但考试只有20人，建议调整到容量为30的教室"

---

## 四、交互体验提升（让对话更自然）

### 4.1 多轮对话上下文（P1）

**功能描述：**  
记住上一轮对话内容，支持指代消解。

**示例对话：**
```
用户："查询张老师的监考安排"
AI：（返回张老师的监考安排）
用户："他下周有空吗？"
AI：（理解"他"指代"张老师"，查询下周的安排）
```

**实现方案：**
1. 在 `chat.py` 中维护会话历史（`messages` 数组）
2. 将历史消息传给 DeepSeek API（Already supported）
3. 在系统提示词中增加指代消解指令

---

### 4.2 模糊查询澄清（P1）

**功能描述：**  
当查询结果有多个匹配时，列出选项让用户选择。

**示例对话：**
```
用户："查询张老师的监考安排"
AI："系统中有3位张老师，请问您要查询哪一位？
     1. 张三（专任教师）
     2. 张四（兼职教师）
     3. 张五（专任教师）"
用户："1"
AI：（查询张三的监考安排）
```

**实现方案：**
1. 工具返回多个匹配结果时，不直接返回数据
2. 生成澄清问题，列出所有选项
3. 等待用户选择，再调用工具查询具体教师

---

### 4.3 操作确认机制（P2）

**功能描述：**  
执行敏感操作前确认，防止误操作。

**示例对话：**
```
用户："把张老师的监考调到周二"
AI："确定要将张老师的监考从周一调整到周二吗？（输入'确定'继续）"
用户："确定"
AI：（执行调整）
```

**实现方案：**
1. 在系统提示词中增加确认指令
2. AI 在调用敏感工具前，先返回确认消息
3. 用户确认后，再调用工具

---

## 五、与现有功能深度集成

### 5.1 自然语言触发手动微调（P2）

**功能描述：**  
用户用自然语言描述调整需求，AI 自动打开手动微调页面并预填参数。

**示例对话：**
```
用户："帮我把周一上午的考试调到下午"
AI：（打开手动微调页面，预填：原时段=周一上午，目标时段=周一下午）
```

**实现方案：**
1. AI 解析用户意图，提取调整参数
2. 前端增加 API：`POST /api/chat/trigger-adjustment`
3. 前端收到指令后，自动跳转到手动微调页面并预填表单

---

### 5.2 自然语言导入数据（P2）

**功能描述：**  
用户请求导入数据，AI 自动下载模板并引导上传。

**示例对话：**
```
用户："导入教师数据"
AI：（返回教师导入模板下载链接）"请下载模板，填写后上传。"
用户：（上传文件）
AI：（调用导入接口）"成功导入30位教师。"
```

---

## 六、报表生成与可视化

### 6.1 教师监考报表（P1）

**功能描述：**  
自动生成每位教师的监考安排报表（Excel）。

**报表内容：**
- 教师姓名、类型、监考场次
- 每场监考的时间、地点、课程

**实现方案：**
1. 调用 `query_teacher_workload` 工具获取数据
2. 调用导出服务生成 Excel
3. 返回下载链接

---

### 6.2 班级考试通知（P1）

**功能描述：**  
自动生成每个班级的考试通知（含时间、地点、监考教师）。

**通知内容：**
- 班级名称
- 考试课程、时间、地点
- 监考教师姓名
- 注意事项（带好准考证、2B铅笔等）

**实现方案：**
1. 调用 `query_class_exams` 工具获取班级考试安排
2. 生成通知文本（可用 LLM 生成更人性化的通知）
3. 导出为 Word/PDF（可选）

---

## 七、实现优先级总结

| 优先级 | 功能数量 | 预计工作量 |
|--------|----------|------------|
| P1 | 7 个 | 约 3-5 天 |
| P2 | 14 个 | 约 7-10 天 |
| P3 | 6 个 | 约 3-5 天 |

---

## 八、技术栈说明

**后端：**
- FastAPI + SQLAlchemy（现有）
- DeepSeek API（现有）
- 新增工具函数（`app/tools/` 目录）

**前端：**
- Vanilla JS（现有）
- SSE 流式输出（现有）
- 新增：操作确认弹窗、澄清选择界面

**数据库：**
- PostgreSQL（现有）
- 现有表结构无需修改

---

## 九、下一步行动

1. **先实现 P1 功能**（预计 3-5 天）
2. **测试每个新工具**（单元测试 + 集成测试）
3. **更新文档**（`PROGRESS.md`、`OPERATIONS.md`）
4. **部署到测试环境**，让实际用户试用
5. **收集反馈**，调整功能优先级

---

**文件路径：** `AI_FEATURE_ROADMAP.md`  
**维护者：** 小白  
**最后更新：** 2026-05-17
